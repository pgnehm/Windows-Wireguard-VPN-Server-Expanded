<Window x:Class="WireGuardServerForWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:WireGuardServerForWindows.Controls"
        xmlns:models="clr-namespace:WireGuardServerForWindows.Models"
        xmlns:local="clr-namespace:WireGuardServerForWindows"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type={x:Type models:MainWindowModel}}"
        Title="Wireguard Server" Height="760" Width="900"
        MinHeight="600" MinWidth="700"
        Icon="/Images/logo.ico"
        Loaded="Window_Loaded">

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:ShortcutCommands.AboutBoxCommand}" Executed="AboutBoxCommand_Executed"/>
    </Window.CommandBindings>

    <Grid Background="#FFF7F8FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="18,18,18,10" Padding="22,18"
                CornerRadius="8" Background="#FF1F4E79">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Wireguard Server" Foreground="White" FontSize="26" FontWeight="Bold"/>
                    <TextBlock Text="Standard VPN management" Foreground="#FFFFE4D8" FontSize="14" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding StatusTitle}" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,18,0,0"/>
                    <TextBlock Text="{Binding StatusSummary}" Foreground="#FFEAF4FF" TextWrapping="Wrap" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Width="180" VerticalAlignment="Center" Margin="24,0,0,0">
                    <ProgressBar Height="12" Minimum="0" Maximum="{Binding RequiredCount, Mode=OneWay}" Value="{Binding CompletedCount, Mode=OneWay}"/>
                    <TextBlock Text="{Binding CompletedCount, StringFormat={}{0} completed}" Foreground="White" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Margin="22,4,22,4">
            <TextBlock Text="Setup steps" FontSize="22" FontWeight="SemiBold" Foreground="#FF263238"/>
            <TextBlock Text="Complete these steps once. After that, use the status window to monitor the server." Foreground="#FF54616B" Margin="0,3,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding PrerequisiteItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="18,7" Padding="6" Background="White" CornerRadius="6" BorderBrush="#FFE1E6EA" BorderThickness="1">
                            <controls:PrerequisiteItemControl/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
