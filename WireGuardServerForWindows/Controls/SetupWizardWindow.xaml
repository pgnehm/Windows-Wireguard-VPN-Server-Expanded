<Window x:Class="WireGuardServerForWindows.Controls.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:WireGuardServerForWindows.Models"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=models:SetupWizardViewModel}"
        Width="760" Height="560"
        MinWidth="680" MinHeight="500"
        Title="Guided setup">
    <Grid Background="#FFF7F8FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF1F4E79" Padding="22" Margin="16,16,16,10" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Guided setup" Foreground="White" FontSize="26" FontWeight="Bold"/>
                <TextBlock Text="{Binding StepCounter}" Foreground="#FFFFE4D8" FontSize="14" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#FFE1E6EA" BorderThickness="1" CornerRadius="6" Margin="16,0,16,10" Padding="22">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding CurrentStep.Title}" FontSize="24" FontWeight="SemiBold" Foreground="#FF263238"/>
                <TextBlock Grid.Row="1" Text="{Binding CurrentStep.StatusText}" TextWrapping="Wrap" Margin="0,8,0,0" FontSize="14"/>
                <TextBlock Grid.Row="2" Text="{Binding CurrentStep.PlainLanguageInstructions}" TextWrapping="Wrap" Margin="0,18,0,0" FontSize="15" Foreground="#FF33424C"/>

                <Border Grid.Row="3" Background="#FFFFF6F0" BorderBrush="#FFFFCFB9" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,20,0,0" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="What to do now" FontWeight="SemiBold" Foreground="#FF263238"/>
                        <TextBlock Text="{Binding CurrentStep.ActionText}" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="#FF33424C"/>
                    </StackPanel>
                </Border>

                <Button Grid.Row="4" Content="{Binding PrimaryActionText}" Command="{Binding RunCurrentStepCommand}"
                        Padding="14,8" HorizontalAlignment="Left" Margin="0,18,0,0"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,16,16">
            <Button Content="Back" Command="{Binding BackCommand}" IsEnabled="{Binding CanGoBack}" Padding="16,7" Margin="0,0,8,0"/>
            <Button Content="Next" Command="{Binding NextCommand}" IsEnabled="{Binding CanGoNext}" Padding="16,7" Margin="0,0,8,0"/>
            <Button Content="Close" IsCancel="True" Padding="16,7"/>
        </StackPanel>
    </Grid>
</Window>
